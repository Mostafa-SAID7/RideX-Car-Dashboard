/**
 * src/constants/index.ts
 *
 * Re-exports from the canonical data and types layers.
 * This file exists only for backward-compatibility during migration.
 * Prefer importing directly from `../data/cars` or `../types`.
 */

export * from '../data/cars';
export * from '../types';
